# v0.1 Smoke Test Report

## Summary
pass — all 7 phases completed cleanly; the end-to-end transport path (claude CLI with stream-json + image) works with subscription auth and session resume round-trips correctly.

## Phase results

1. **Clean build**: ok. `make clean && make build` completed in 9.5s wall (swift build 7.43s). Final line `==> built Clicky.app`. No warnings.

2. **Bundle structure**: ok.
   - Executable: `Clicky.app/Contents/MacOS/clicky` (454 KB, arm64).
   - Info.plist key fields:
     - `CFBundleIdentifier = com.proyecto26.clicky`
     - `LSUIElement = true`
     - `NSScreenCaptureUsageDescription` set (explains capture intent)
     - `NSMicrophoneUsageDescription` set
     - `CFBundleShortVersionString = 0.1.0`, `LSMinimumSystemVersion = 14.2`
   - codesign: `Identifier=com.proyecto26.clicky`, `Signature=adhoc`, `Format=app bundle with Mach-O thin (arm64)`, `flags=0x10002(adhoc,runtime)`.

3. **Launch + presence**: ok. `open Clicky.app` succeeded. `pgrep -l clicky` -> `13283 clicky`. System Events reported the `clicky` process. No dock icon (LSUIElement honored).

4. **Log stream**: empty (as expected). 30s capture of `subsystem == "com.proyecto26.clicky"` at debug level produced only the filter banner — the app emits no logs in the idle state, and no turn was triggered. Non-blocking.

5. **Direct transport**: ok.
   - Screenshot: 191,460 bytes jpeg (800x600 region).
   - stream-json envelope: 255,524 bytes.
   - `apiKeySource = "none"` (subscription auth, not API key) — PASSES the hard gate.
   - `session_id = e1096bc8-69a7-4a4e-b089-58be6b0c45b8` (valid UUID).
   - `text_delta` events: 5.
   - `"subtype":"success"` present; `is_error=false`; `duration_ms=2854`; `stop_reason=end_turn`.
   - Reply text: "This is a VS Code editor window showing a project called \"Clicky\" with its **AGENTS.md** file open, which contains AI agent instructions for a macOS menu bar companion app that uses voice controls and Claude AI."
   - Tool gating worked — the disallowed list was respected, no Bash/Read/etc invoked.

6. **Session resume**: ok.
   - Session UUID: `07A9AF05-7D8F-4ACB-8F50-CFDB878A13F3`.
   - Turn 1 reply: `Got it — I'll remember 73.`
   - Turn 2 reply: `73`
   - Memory persisted across `--resume`; the model correctly recalled the number.

7. **Shutdown**: ok. `osascript ... quit` succeeded; `pgrep -l clicky` after 2s returned no process.

## Failures or surprises

None. One minor note: the `log stream` predicate in Phase 4 returned no events because the app emits logs only during an active turn, which cannot be triggered from the terminal without UI interaction or a global hotkey — this matches the plan's guidance.

The Phase 5 reply described the user's current VS Code window rather than the captured region's geometric content, which is expected — `screencapture -R 0,0,800,600` sampled the top-left of the active display, which happened to be the editor.

## Recommendation

ship v0.1. The build is reproducible, the bundle is correctly structured for a menu-bar-only app, the CLI transport path works with subscription auth and images, session resume is intact, and shutdown is clean. No blockers observed.
